# Core runtime. Versions verified as current at build time (July 2026).
pandas>=2.2
numpy>=1.26
PyYAML>=6.0

# IBKR connectivity - required only to actually connect to TWS/Gateway.
# ib_async is the maintained successor to ib_insync, whose author died in 2024
# and whose project receives no further updates. 2.1.0 (Dec 2025), Python >=3.10.
# Everything else in this system - backtests, the full test suite, the CLI's
# offline modes - runs without it.
ib_async>=2.1.0

# Testing.
pytest>=8.0
