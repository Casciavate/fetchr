[pytest]
# Run from the repository root: python -m pytest trading_bot/tests -q
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = --strict-markers -q
filterwarnings =
    error::DeprecationWarning:trading_bot.*
